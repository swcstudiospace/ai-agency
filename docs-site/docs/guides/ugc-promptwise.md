# UGC with PromptWise

1. `promptwise_build_ugc_brief`
2. `promptwise_run_ugc_job` (browser via Hermes bridge or API)
3. Fal fallback

UI-first product; log into PromptWise once on the bridge browser profile.
